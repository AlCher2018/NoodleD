<Window x:Class="WpfApplication1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApplication1"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525"  FontSize="24">
    <StackPanel>
        <ListBox x:Name="lst">
            <ListBoxItem>qqq</ListBoxItem>
            <ListBoxItem>www</ListBoxItem>
            <ListBoxItem>eee</ListBoxItem>
            <ListBoxItem>rrr</ListBoxItem>
            <ListBoxItem>ttt</ListBoxItem>
        </ListBox>
        <CheckBox x:Name="checkBox1"/>
        <TextBox Text="sdkfjsdkfsdf">
           <TextBox.Style>
                <Style TargetType="TextBox">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ElementName=lst, Path=SelectedIndex}"
                        Value="2">
                            <Setter Property="Background" Value="Cyan"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>

        </TextBox>
    </StackPanel>
</Window>
